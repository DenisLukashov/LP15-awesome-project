language: python
python:
  - "3.7"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
skip_commits:
  files:
    - migrations/*
script:
# unit test
  - pytest
# linting test
  - flake8
